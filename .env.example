# Copie este arquivo para .env e substitua somente os valores à direita.
# Nunca versione o arquivo .env.

# Obrigatória para o GeneralLLM do workflow real (gpt-4.1-mini).
OPENAI_API_KEY=sua_chave_openai

# Obrigatória para gerar a resposta final com o adapter LoRA treinado.
# Use um caminho absoluto para o diretório salvo pelo treinamento.
QWEN_LORA_ADAPTER_PATH=/caminho/absoluto/para/o/adapter-lora

# Opcional: o runtime usa este valor como padrão caso não seja informado.
QWEN_BASE_MODEL=unsloth/Qwen3.5-4B
# Opcional para testar uma GPU pequena: mantém módulos excedentes em CPU/FP32.
# Exige bastante RAM e a geração será muito mais lenta.
QWEN_ENABLE_CPU_OFFLOAD=false
# Limite de RAM destinado ao offload; use apenas junto com a opção acima.
QWEN_CPU_OFFLOAD_MAX_MEMORY=12GiB
# Limite máximo de tokens na resposta final. Para teste local, experimente 32 ou 64.
QWEN_MAX_NEW_TOKENS=256
# Revisões adicionais após a resposta inicial: 0 desativa revisões; 1 mantém o padrão.
MAX_RESPONSE_REVISIONS=1
# Provider da resposta final: qwen (padrão) ou openai, para validar o fluxo sem GPU.
FINAL_ANSWER_PROVIDER=qwen
# Usado somente quando FINAL_ANSWER_PROVIDER=openai.
OPENAI_FINAL_MODEL=gpt-4.1-mini

# --- API HTTP (FastAPI) ---
# Banco SQLite da demo; inicializado no startup da API.
CLINICAL_DATABASE=clinical_demo.db
# true = LLMs determinísticas (sem OpenAI/Qwen). Use em testes e demos locais.
API_USE_FAKES=false
# Identidade e pacientes autorizados vêm só do servidor (nunca do browser).
DEMO_REQUESTER_ID=demo-clinician
DEMO_AUTHORIZED_PATIENT_IDS=P-042
